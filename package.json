{
  "name": "explorer",
  "version": "1.0.0",
  "description": "Back end for sportnumerics explorer",
  "scripts": {
    "deploy": "sls deploy --stage=dev --verbose",
    "test-divs": "sls invoke local --function divs --stage dev",
    "test-teams": "sls invoke local --function teams --stage dev --path fixtures/invocations/teams.json"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "aws-sdk": "^2.4.13",
    "config": "^1.21.0",
    "bluebird": "^3.4.6",
    "isomorphic-fetch": "^2.2.1"
  },
  "devDependencies": {
    "serverless": "~1.6.1"
  }
}
